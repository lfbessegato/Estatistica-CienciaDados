# Dados
dados = c(3, 4, 5, 6, 12)

# Amplitude
print(diff(range(dados)))

# Variância
print(var(dados))

# Desvio-padrão
print(sd(dados))

# Coeficiente de variação
print(sd(dados) / mean(dados) * 100)
